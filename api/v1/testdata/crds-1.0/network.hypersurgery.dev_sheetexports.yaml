---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: sheetexports.network.hypersurgery.dev
spec:
  group: network.hypersurgery.dev
  names:
    categories:
    - hypersurgery
    kind: SheetExport
    listKind: SheetExportList
    plural: sheetexports
    singular: sheetexport
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.scopeRef
      name: Scope
      type: string
    - jsonPath: .status.rows
      name: Rows
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.lastExportTime
      name: Last export
      type: date
    - jsonPath: .status.url
      name: URL
      priority: 1
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          SheetExport mirrors the subnets of a NetworkScope into a Google Sheet, for people who
          still want to look at a table. The sheet is written by the operator and never read back.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SheetExport
            properties:
              credentialsSecretRef:
                description: credentialsSecretRef holds the Google service account
                  JSON.
                properties:
                  key:
                    default: credentials.json
                    description: key holding the Google service account JSON.
                    type: string
                  name:
                    description: name of the Secret.
                    type: string
                  namespace:
                    description: namespace of the Secret.
                    type: string
                required:
                - name
                - namespace
                type: object
              extraTagColumns:
                description: extraTagColumns adds one column per tag key, after the
                  standard columns.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              refreshInterval:
                default: 5m
                description: refreshInterval is how often the sheet is rewritten.
                type: string
              scopeRef:
                description: scopeRef is the NetworkScope whose subnets are exported.
                maxLength: 253
                minLength: 1
                type: string
              sheetName:
                default: Subnets
                description: |-
                  sheetName is the tab that holds the inventory. It is created if missing and fully
                  rewritten on every export, so people should not keep their own data in it.
                type: string
              spreadsheetID:
                description: |-
                  spreadsheetID is the ID from the spreadsheet URL
                  (docs.google.com/spreadsheets/d/<spreadsheetID>/edit). The spreadsheet must be shared
                  with the service account as an editor.
                type: string
            required:
            - credentialsSecretRef
            - scopeRef
            - spreadsheetID
            type: object
          status:
            description: status defines the observed state of SheetExport
            properties:
              conditions:
                description: 'conditions: Ready is True when the last export succeeded.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastExportTime:
                description: lastExportTime is when the sheet was last written.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the generation of the spec that
                  was last exported.
                format: int64
                type: integer
              rows:
                description: rows is the number of subnets in the sheet, excluding
                  the header.
                format: int32
                type: integer
              url:
                description: url links to the exported sheet.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.scopeRef
      name: Scope
      type: string
    - jsonPath: .status.rows
      name: Rows
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.lastExportTime
      name: Last export
      type: date
    - jsonPath: .status.url
      name: URL
      priority: 1
      type: string
    deprecated: true
    deprecationWarning: 'network.hypersurgery.dev/v1beta1 is deprecated: use network.hypersurgery.dev/v1,
      which has the same fields. v1beta1 is served until at least 1.2 and six months
      after 1.0'
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          SheetExport mirrors the subnets of a NetworkScope into a Google Sheet, for people who
          still want to look at a table. The sheet is written by the operator and never read back.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SheetExport
            properties:
              credentialsSecretRef:
                description: credentialsSecretRef holds the Google service account
                  JSON.
                properties:
                  key:
                    default: credentials.json
                    description: key holding the Google service account JSON.
                    type: string
                  name:
                    description: name of the Secret.
                    type: string
                  namespace:
                    description: namespace of the Secret.
                    type: string
                required:
                - name
                - namespace
                type: object
              extraTagColumns:
                description: extraTagColumns adds one column per tag key, after the
                  standard columns.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: atomic
              refreshInterval:
                default: 5m
                description: refreshInterval is how often the sheet is rewritten.
                type: string
              scopeRef:
                description: scopeRef is the NetworkScope whose subnets are exported.
                type: string
              sheetName:
                default: Subnets
                description: |-
                  sheetName is the tab that holds the inventory. It is created if missing and fully
                  rewritten on every export, so people should not keep their own data in it.
                type: string
              spreadsheetID:
                description: |-
                  spreadsheetID is the ID from the spreadsheet URL
                  (docs.google.com/spreadsheets/d/<spreadsheetID>/edit). The spreadsheet must be shared
                  with the service account as an editor.
                type: string
            required:
            - credentialsSecretRef
            - scopeRef
            - spreadsheetID
            type: object
          status:
            description: status defines the observed state of SheetExport
            properties:
              conditions:
                description: 'conditions: Ready is True when the last export succeeded.'
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastExportTime:
                description: lastExportTime is when the sheet was last written.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the generation of the spec that
                  was last exported.
                format: int64
                type: integer
              rows:
                description: rows is the number of subnets in the sheet, excluding
                  the header.
                format: int32
                type: integer
              url:
                description: url links to the exported sheet.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
