# API Reference

<!--
Generated by `make api-docs` from the Go types in api/v1 with crd-ref-docs. Do not edit: change
the doc comments and markers in api/v1 instead. CI fails when this file is out of date.
-->

This is the field-by-field reference of `network.hypersurgery.dev/v1`, the API the
[compatibility promise](../api-compatibility.md) covers. `v1beta1` is still served, deprecated,
with the same fields; the [upgrade guide](../operations/upgrades.md#upgrading-from-09-to-10)
says how objects move from it. The kinds, what they are for and examples are in the
[README](../../README.md#crds) and [`examples/`](../../examples/).

## Packages
- [network.hypersurgery.dev/v1](#networkhypersurgerydevv1)


## network.hypersurgery.dev/v1

Package v1 contains API Schema definitions for the network.hypersurgery.dev v1 API group:
the stable, cloud-neutral API (ADR 0002 §10). It is the storage version and the hub every
other served version converts through; docs/api-compatibility.md says what may change within
it.

Every kind that talks about the cloud names its provider, directly (NetworkScope, Network,
Subnet) or through the scope it refers to (SubnetClaim, ResourceImport). Provider is an open
enum: clients must treat a value they do not know as "a provider this client does not know"
and skip the object instead of failing.

### Resource Types
- [Network](#network)
- [NetworkList](#networklist)
- [NetworkScope](#networkscope)
- [NetworkScopeList](#networkscopelist)
- [ResourceImport](#resourceimport)
- [ResourceImportList](#resourceimportlist)
- [SheetExport](#sheetexport)
- [SheetExportList](#sheetexportlist)
- [Subnet](#subnet)
- [SubnetClaim](#subnetclaim)
- [SubnetClaimList](#subnetclaimlist)
- [SubnetList](#subnetlist)



#### AWSAccount



AWSAccount is how the operator reaches an AWS account.



_Appears in:_
- [Account](#account)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `roleARN` _string_ | roleARN is assumed (sts:AssumeRole) to read the account. When empty, the operator's own<br />credentials (EKS Pod Identity / IRSA) are used, which only works for the account the<br />operator runs in. |  | Pattern: `^arn:aws[a-z-]*:iam::[0-9]\{12\}:role/.+$` <br />Optional: \{\} <br /> |
| `externalID` _string_ | externalID is passed to sts:AssumeRole when the role trust policy requires it. |  | Optional: \{\} <br /> |
| `writeRoleARN` _string_ | writeRoleARN is assumed for SubnetClaims in Create mode and for ResourceImports. It is<br />separate from roleARN so that discovery never carries write permissions. When empty,<br />claims for an account reached through roleARN can only be allocated, not created. |  | Pattern: `^arn:aws[a-z-]*:iam::[0-9]\{12\}:role/.+$` <br />Optional: \{\} <br /> |


#### AWSClaimOptions



AWSClaimOptions are the settings of subnets created on AWS.



_Appears in:_
- [SubnetClaimSpec](#subnetclaimspec)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `routeTableID` _string_ | routeTableID is associated with every created subnet. Leave empty for the VPC main table. |  | Pattern: `^rtb-[0-9a-f]+$` <br />Optional: \{\} <br /> |
| `mapPublicIPOnLaunch` _boolean_ | mapPublicIPOnLaunch sets the subnet attribute of the same name. |  | Optional: \{\} <br /> |


#### AWSNetworkStatus



AWSNetworkStatus is what only an AWS VPC has.



_Appears in:_
- [NetworkStatus](#networkstatus)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `isDefault` _boolean_ | isDefault is true for the default VPC of the region. |  | Optional: \{\} <br /> |


#### AWSScope



AWSScope holds scope-wide settings for AWS. It has none yet; it exists so that a scope can
say `aws: {}` and so that AWS-only settings have a place that is not a neutral field.



_Appears in:_
- [NetworkScopeSpec](#networkscopespec)



#### AWSSubnetStatus



AWSSubnetStatus is what only an AWS subnet has.



_Appears in:_
- [SubnetStatus](#subnetstatus)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `availabilityZoneID` _string_ | availabilityZoneID is the account-independent AZ ID, e.g. euc1-az2. |  | Optional: \{\} <br /> |
| `public` _boolean_ | public is true when the subnet's route table has a default route to an internet gateway. |  | Optional: \{\} <br /> |
| `routeTableID` _string_ | routeTableID is the explicitly associated route table, or the VPC main route table. |  | Optional: \{\} <br /> |


#### Account



Account is one account of the provider: an AWS account ID today, a GCP project or an Azure
subscription in the releases that add them. Its identity settings are in the member named
after the scope's provider.



_Appears in:_
- [NetworkScopeSpec](#networkscopespec)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `id` _string_ | id is the account's ID at the provider. For AWS it is the 12-digit account ID; the<br />format is checked per provider on the scope. |  | MaxLength: 128 <br />MinLength: 1 <br />Required: \{\} <br /> |
| `regions` _string array_ | regions overrides spec.regions for this account. Each region is listed once. |  | Optional: \{\} <br /> |
| `aws` _[AWSAccount](#awsaccount)_ | aws holds how the operator reaches the account on AWS. Only valid in a scope with<br />provider AWS. |  | Optional: \{\} <br /> |


#### AccountDefault



AccountDefault gives an account a set of fallback tags.



_Appears in:_
- [AutoImportPolicy](#autoimportpolicy)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `account` _string_ | account is the account ID, in the provider's format; checked on the scope. |  | MaxLength: 128 <br />MinLength: 1 <br />Required: \{\} <br /> |
| `tags` _object (keys:string, values:string)_ | tags are applied to resources in that account when nothing more specific matched. |  | MinProperties: 1 <br />Required: \{\} <br /> |


#### AutoImportMode

_Underlying type:_ _string_

AutoImportMode says how far the policy goes on its own. Unlike Provider it is a closed enum:
a client may rely on knowing every value, so adding one is a behaviour change that needs a new
API version or an opt-in field (docs/api-compatibility.md).

_Validation:_
- Enum: [Off DryRun Apply]

_Appears in:_
- [AutoImportPolicy](#autoimportpolicy)

| Field | Description |
| --- | --- |
| `Off` | AutoImportOff leaves unmanaged resources alone. They are still counted and alerted on.<br /> |
| `DryRun` | AutoImportDryRun creates the ResourceImport objects with dryRun set, so a day of them<br />can be reviewed before anything in the cloud is touched.<br /> |
| `Apply` | AutoImportApply creates real imports: the resources get tagged.<br /> |


#### AutoImportPolicy



AutoImportPolicy decides which unmanaged resources get tagged, and with what.



_Appears in:_
- [NetworkScopeSpec](#networkscopespec)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `mode` _[AutoImportMode](#autoimportmode)_ | mode is Off (report only), DryRun (record what would be applied) or Apply. | Off | Enum: [Off DryRun Apply] <br />Optional: \{\} <br /> |
| `namespace` _string_ | namespace holds the generated ResourceImport objects. | default | Optional: \{\} <br /> |
| `fromCreator` _[CreatorRule](#creatorrule) array_ | fromCreator derives tags from the principal that created the resource, which the<br />provider's audit trail reports (CloudTrail on AWS). Rules are tried in order and the first match wins. |  | Optional: \{\} <br /> |
| `inheritFromNetwork` _string array_ | inheritFromNetwork copies these tag keys from the parent network onto an unmanaged<br />subnet. Tried after fromCreator, for the common case of a subnet added by hand to a<br />network that already has an owner. |  | Optional: \{\} <br /> |
| `accountDefaults` _[AccountDefault](#accountdefault) array_ | accountDefaults are the last resort, per account. An account has at most one entry. |  | MaxItems: 1000 <br />Optional: \{\} <br /> |
| `skip` _[SkipRule](#skiprule) array_ | skip keeps the policy away from matching resources entirely. |  | Optional: \{\} <br /> |
| `requiredTags` _string array_ | requiredTags must all be resolved before anything is imported. Defaults to the owner<br />tag: a resource nobody owns stays unmanaged, and the alert asks a human to decide. |  | Optional: \{\} <br /> |
| `managedTag` _string_ | managedTag and managedValue are added to every generated import, because that is what<br />makes the resource part of the inventory. managedTag defaults per provider; on AWS to<br />hs/managed. |  | Optional: \{\} <br /> |
| `managedValue` _string_ |  | true | Optional: \{\} <br /> |


#### Capability

_Underlying type:_ _string_

Capability is something a provider can do, as the operator runs it (ADR 0002 §8). The
dashboard and the webhooks read it from NetworkScope.status.capabilities instead of guessing
from the provider's name. More values may be added; a client must ignore values it does not
know.



_Appears in:_
- [NetworkScopeStatus](#networkscopestatus)

| Field | Description |
| --- | --- |
| `CreateSubnet` | CapabilityCreateSubnet: SubnetClaims in Create mode can create subnets. Whether writes are<br />switched on (--enable-writes) is a separate, operator-wide setting.<br /> |
| `ChangeEvents` | CapabilityChangeEvents: the operator receives the provider's change events, so changes<br />show up within seconds instead of at the next resync.<br /> |
| `IPUsage` | CapabilityIPUsage: the provider reports free addresses per subnet. Individual subnets may<br />still report them as unknown.<br /> |


#### ClaimMode

_Underlying type:_ _string_

ClaimMode says how far the operator goes with a claim. Unlike Provider it is a closed enum:
a client may rely on knowing every value, so adding one is a behaviour change that needs a new
API version or an opt-in field (docs/api-compatibility.md).

_Validation:_
- Enum: [Allocate Create]

_Appears in:_
- [SubnetClaimSpec](#subnetclaimspec)

| Field | Description |
| --- | --- |
| `Allocate` | ClaimModeAllocate only reserves CIDRs and writes them to the status. Nothing is<br />created in the cloud: Terraform-first teams create the subnets from the status themselves.<br /> |
| `Create` | ClaimModeCreate reserves CIDRs and creates the subnets.<br /> |


#### CreatorRule



CreatorRule maps the principal that created a resource to the tags it should carry.



_Appears in:_
- [AutoImportPolicy](#autoimportpolicy)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `principalPrefix` _string_ | principalPrefix is matched against the principal as the provider's audit trail reports<br />it; on AWS the CloudTrail principal, e.g.<br />"arn:aws:sts::222222222222:assumed-role/payments-deploy/". A prefix, not a pattern:<br />the operator never guesses an owner from a partial match in the middle of an ARN. |  | MinLength: 1 <br />Required: \{\} <br /> |
| `tags` _object (keys:string, values:string)_ | tags are applied when the prefix matches. |  | MinProperties: 1 <br />Required: \{\} <br /> |


#### Network



Network is a network discovered by a NetworkScope: an AWS VPC. It is read-only for users.
On AWS the object is named after the VPC ID.



_Appears in:_
- [NetworkList](#networklist)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `Network` | | |
| `metadata` _[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#objectmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  | Optional: \{\} <br /> |
| `spec` _[NetworkSpec](#networkspec)_ | spec identifies the network |  | Required: \{\} <br /> |
| `status` _[NetworkStatus](#networkstatus)_ | status is the observed state of the network |  | Optional: \{\} <br /> |


#### NetworkList



NetworkList contains a list of Network





| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `NetworkList` | | |
| `metadata` _[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#listmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  |  |
| `items` _[Network](#network) array_ |  |  |  |


#### NetworkScope



NetworkScope selects the accounts and regions of one provider whose networks and subnets
are discovered.



_Appears in:_
- [NetworkScopeList](#networkscopelist)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `NetworkScope` | | |
| `metadata` _[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#objectmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  | Optional: \{\} <br /> |
| `spec` _[NetworkScopeSpec](#networkscopespec)_ | spec defines the desired state of NetworkScope |  | Required: \{\} <br /> |
| `status` _[NetworkScopeStatus](#networkscopestatus)_ | status defines the observed state of NetworkScope |  | Optional: \{\} <br /> |


#### NetworkScopeList



NetworkScopeList contains a list of NetworkScope





| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `NetworkScopeList` | | |
| `metadata` _[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#listmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  |  |
| `items` _[NetworkScope](#networkscope) array_ |  |  |  |


#### NetworkScopeSpec



NetworkScopeSpec defines which accounts and regions of one provider are discovered.



_Appears in:_
- [NetworkScope](#networkscope)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `provider` _[Provider](#provider)_ | provider is the cloud the scope discovers. It cannot change: a scope does not move<br />clouds, a new scope does. More values are added by the releases that implement them. |  | Enum: [AWS] <br />Required: \{\} <br /> |
| `accounts` _[Account](#account) array_ | accounts to discover. |  | MaxItems: 1000 <br />MinItems: 1 <br />Required: \{\} <br /> |
| `regions` _string array_ | regions to discover in every account (unless the account overrides them): AWS regions<br />such as eu-central-1. Each region is listed once. |  | MinItems: 1 <br />Required: \{\} <br /> |
| `networkSelector` _[NetworkSelector](#networkselector)_ | networkSelector limits discovery to the networks (AWS VPCs) it selects. When unset or<br />empty, every network is discovered. |  | Optional: \{\} <br /> |
| `requiredSubnetTags` _string array_ | requiredSubnetTags are tag keys every subnet must have. Missing keys are reported in the<br />Subnet status and as metrics. |  | Optional: \{\} <br /> |
| `tagKeys` _[TagKeys](#tagkeys)_ | tagKeys maps inventory fields (owner, env, tier) to tag keys. Unset keys default per<br />provider; on AWS to hs/owner, hs/env and hs/tier. |  | Optional: \{\} <br /> |
| `resyncInterval` _[Duration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#duration-v1-meta)_ | resyncInterval is how often the full inventory is refreshed. | 10m | Optional: \{\} <br /> |
| `discoverUnmanaged` _boolean_ | discoverUnmanaged also lists the networks the selector does not match, and their<br />subnets, so resources nobody has claimed are counted and can be imported instead of<br />staying invisible. They are reported and alerted on, never mirrored as objects. | true | Optional: \{\} <br /> |
| `autoImport` _[AutoImportPolicy](#autoimportpolicy)_ | autoImport tags unmanaged resources on its own, deriving the tags from who created the<br />resource, from its network, or from the account. Off unless configured. |  | Optional: \{\} <br /> |
| `namespaceSelector` _[LabelSelector](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#labelselector-v1-meta)_ | namespaceSelector selects the namespaces whose SubnetClaims and ResourceImports may refer<br />to this scope. Referring to a scope is what makes the operator use its identities,<br />including the write identities, on the object's behalf, so this is what keeps one team's<br />namespace from creating subnets in, or tagging, another team's accounts.<br />Select namespaces by name with the kubernetes.io/metadata.name label, which the API<br />server sets on every namespace and nobody can change, or by a label only cluster<br />administrators can set. An empty selector (\{\}) allows every namespace on purpose.<br />When unset, no namespace may use the scope. (In aws.hypersurgery/v1alpha1 unset allowed<br />every namespace; migrated scopes get an explicit \{\} instead.) |  | Optional: \{\} <br /> |
| `aws` _[AWSScope](#awsscope)_ | aws holds scope-wide AWS settings. Only valid with provider AWS. |  | Optional: \{\} <br /> |


#### NetworkScopeStatus



NetworkScopeStatus defines the observed state of NetworkScope.



_Appears in:_
- [NetworkScope](#networkscope)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `observedGeneration` _integer_ | observedGeneration is the generation of the spec that was last synced. |  | Optional: \{\} <br /> |
| `lastSyncTime` _[Time](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#time-v1-meta)_ | lastSyncTime is when the last full sync finished. |  | Optional: \{\} <br /> |
| `networks` _integer_ | networks is the number of networks discovered across all targets. |  | Optional: \{\} <br /> |
| `subnets` _integer_ | subnets is the number of subnets discovered across all targets. |  | Optional: \{\} <br /> |
| `unmanaged` _integer_ | unmanaged is the number of discovered resources without the managed tag, across all<br />targets. Anything above zero is something nobody has taken responsibility for. |  | Optional: \{\} <br /> |
| `capabilities` _[Capability](#capability) array_ | capabilities lists what the scope's provider can do as the operator runs it:<br />CreateSubnet, ChangeEvents, IPUsage. More values may be added; ignore unknown ones. |  | Optional: \{\} <br /> |
| `ownership` _[Ownership](#ownership)_ | ownership says where the provider keeps ownership metadata of networks and subnets. |  | Optional: \{\} <br /> |
| `targets` _[TargetStatus](#targetstatus) array_ | targets reports each account/region pair. |  | Optional: \{\} <br /> |
| `conditions` _[Condition](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#condition-v1-meta) array_ | conditions: Ready is True when every target synced successfully. |  | Optional: \{\} <br /> |


#### NetworkSelector



NetworkSelector limits discovery to some of the provider's networks.



_Appears in:_
- [NetworkScopeSpec](#networkscopespec)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `matchTags` _object (keys:string, values:string)_ | matchTags selects the networks that carry all of these tags. An empty value matches any<br />value of the key. All subnets of a selected network are discovered, including untagged<br />ones, so they show up in compliance reports. |  | Optional: \{\} <br /> |


#### NetworkSpec



NetworkSpec identifies the observed network. It is written by the operator, not by users.



_Appears in:_
- [Network](#network)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `provider` _[Provider](#provider)_ | provider is the provider of the scope that discovered the network. |  | Enum: [AWS] <br /> |
| `id` _string_ | id is the provider's ID of the network; for AWS the VPC ID. |  |  |
| `account` _string_ | account is the account that owns the network. |  |  |
| `region` _string_ | region of the network. Empty for providers whose networks are global. |  | Optional: \{\} <br /> |


#### NetworkStatus



NetworkStatus is the observed state of the network.



_Appears in:_
- [Network](#network)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `name` _string_ | name is the value of the Name tag. |  | Optional: \{\} <br /> |
| `state` _string_ | state is the provider's state of the network. |  | Optional: \{\} <br /> |
| `cidrBlocks` _string array_ | cidrBlocks are the associated IPv4 CIDR blocks, primary first. |  | Optional: \{\} <br /> |
| `ipv6CIDRBlocks` _string array_ | ipv6CIDRBlocks are the associated IPv6 CIDR blocks. |  | Optional: \{\} <br /> |
| `owner` _string_ | owner is read from the owner tag configured in the NetworkScope. |  | Optional: \{\} <br /> |
| `env` _string_ | env is read from the env tag configured in the NetworkScope. |  | Optional: \{\} <br /> |
| `tags` _object (keys:string, values:string)_ | tags are the network's tags at the provider. |  | Optional: \{\} <br /> |
| `subnets` _integer_ | subnets is the number of discovered subnets. |  | Optional: \{\} <br /> |
| `totalIPs` _integer_ | totalIPs is the sum of usable IPv4 addresses in all subnets. Unset when unknown. |  | Optional: \{\} <br /> |
| `availableIPs` _integer_ | availableIPs is the sum of free IPv4 addresses in all subnets. Unset when the free<br />addresses of any subnet are unknown. |  | Optional: \{\} <br /> |
| `overlapsWith` _string array_ | overlapsWith lists networks in the same NetworkScope whose CIDRs overlap this one, as<br />"<account>/<region>/<id>". Overlaps break peering and transit routing. |  | Optional: \{\} <br /> |
| `aws` _[AWSNetworkStatus](#awsnetworkstatus)_ | aws holds what only an AWS VPC has. |  | Optional: \{\} <br /> |


#### Ownership



Ownership is the ownership model of a provider, per kind of resource.



_Appears in:_
- [NetworkScopeStatus](#networkscopestatus)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `networks` _[OwnershipModel](#ownershipmodel)_ | networks is where the metadata of a network is kept. |  | Optional: \{\} <br /> |
| `subnets` _[OwnershipModel](#ownershipmodel)_ | subnets is where the metadata of a subnet is kept. |  | Optional: \{\} <br /> |


#### OwnershipModel

_Underlying type:_ _string_

OwnershipModel says where a provider keeps the ownership metadata (owner, env, tier, the
managed marker) of a kind of resource (ADR 0002 §6). More values may be added.



_Appears in:_
- [Ownership](#ownership)

| Field | Description |
| --- | --- |
| `ResourceTags` | OwnershipResourceTags keeps the metadata on the resource itself: AWS tags, Azure tags on<br />a VNet, GCP Resource Manager tag bindings.<br /> |
| `ParentNetworkTags` | OwnershipParentNetworkTags keeps the metadata of a subnet on its network, one entry per<br />subnet: Azure subnets, which cannot carry tags.<br /> |


#### OwnershipSource

_Underlying type:_ _string_

OwnershipSource says where the ownership values of one subnet were read from.



_Appears in:_
- [SubnetStatus](#subnetstatus)

| Field | Description |
| --- | --- |
| `Subnet` | OwnershipSourceSubnet: the subnet's own metadata (on the subnet, or its own entry on the<br />parent network).<br /> |
| `Network` | OwnershipSourceNetwork: the subnet has no metadata of its own and inherits its network's.<br /> |


#### Provider

_Underlying type:_ _string_

Provider names the cloud a NetworkScope discovers. It is an open enum: releases that
implement another cloud add its value, so a client must treat a value it does not know as a
provider it does not know and skip the object, not fail on it.

_Validation:_
- Enum: [AWS]

_Appears in:_
- [NetworkScopeSpec](#networkscopespec)
- [NetworkSpec](#networkspec)
- [SubnetSpec](#subnetspec)

| Field | Description |
| --- | --- |
| `AWS` | ProviderAWS is Amazon Web Services: accounts, VPCs and subnets.<br /> |


#### ResourceImport



ResourceImport takes an existing network or subnet under management by tagging it.
The resource itself, and every tag the import does not name, is left exactly as it was.



_Appears in:_
- [ResourceImportList](#resourceimportlist)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `ResourceImport` | | |
| `metadata` _[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#objectmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  | Optional: \{\} <br /> |
| `spec` _[ResourceImportSpec](#resourceimportspec)_ | spec defines the tags to apply |  | Required: \{\} <br /> |
| `status` _[ResourceImportStatus](#resourceimportstatus)_ | status defines the observed state of ResourceImport |  | Optional: \{\} <br /> |


#### ResourceImportList



ResourceImportList contains a list of ResourceImport





| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `ResourceImportList` | | |
| `metadata` _[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#listmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  |  |
| `items` _[ResourceImport](#resourceimport) array_ |  |  |  |


#### ResourceImportSpec



ResourceImportSpec asks for tags to be applied to one existing network or subnet.



_Appears in:_
- [ResourceImport](#resourceimport)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `scopeRef` _string_ | scopeRef is the NetworkScope that supplies the credentials; the account and region<br />below must be part of it. |  | MaxLength: 253 <br />MinLength: 1 <br />Required: \{\} <br /> |
| `account` _string_ | account is the account that owns the resource, in the provider's format (for AWS the<br />12-digit account ID). |  | MinLength: 1 <br />Required: \{\} <br /> |
| `region` _string_ | region of the resource. Required for regional resources, which on AWS is every one. |  | Optional: \{\} <br /> |
| `resourceID` _string_ | resourceID is the provider ID of the network or subnet to tag: for AWS a VPC or subnet<br />ID. |  | MinLength: 1 <br />Required: \{\} <br /> |
| `tags` _object (keys:string, values:string)_ | tags are applied to the resource. Tags with other keys are left alone: the operator<br />only ever adds tags, it never removes one. |  | MinProperties: 1 <br />Required: \{\} <br /> |
| `requestedBy` _string_ | requestedBy records who asked for the import: a person, a team, or "auto-import policy". |  | Optional: \{\} <br /> |
| `dryRun` _boolean_ | dryRun records what would be applied without touching the cloud. The auto-import policy sets<br />it in DryRun mode, so a day of imports can be reviewed before anything is tagged. |  | Optional: \{\} <br /> |


#### ResourceImportStatus



ResourceImportStatus is the observed state of the import.



_Appears in:_
- [ResourceImport](#resourceimport)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `observedGeneration` _integer_ | observedGeneration is the spec generation the status refers to. |  | Optional: \{\} <br /> |
| `state` _string_ | state is Pending, Applied, Skipped (dry run) or Failed. |  | Optional: \{\} <br /> |
| `appliedTags` _object (keys:string, values:string)_ | appliedTags are the tags that reached the provider. |  | Optional: \{\} <br /> |
| `appliedTime` _[Time](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#time-v1-meta)_ | appliedTime is when they were applied. |  | Optional: \{\} <br /> |
| `error` _string_ | error is the last failure, empty otherwise. |  | Optional: \{\} <br /> |
| `conditions` _[Condition](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#condition-v1-meta) array_ | conditions: Ready is True once the import is settled, whether it applied the tags or<br />deliberately did nothing in a dry run. |  | Optional: \{\} <br /> |


#### SecretKeyRef



SecretKeyRef points at one key of a Secret.



_Appears in:_
- [SheetExportSpec](#sheetexportspec)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `name` _string_ | name of the Secret. |  | Required: \{\} <br /> |
| `namespace` _string_ | namespace of the Secret. |  | Required: \{\} <br /> |
| `key` _string_ | key holding the Google service account JSON. | credentials.json | Optional: \{\} <br /> |


#### SheetExport



SheetExport mirrors the subnets of a NetworkScope into a Google Sheet, for people who
still want to look at a table. The sheet is written by the operator and never read back.



_Appears in:_
- [SheetExportList](#sheetexportlist)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `SheetExport` | | |
| `metadata` _[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#objectmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  | Optional: \{\} <br /> |
| `spec` _[SheetExportSpec](#sheetexportspec)_ | spec defines the desired state of SheetExport |  | Required: \{\} <br /> |
| `status` _[SheetExportStatus](#sheetexportstatus)_ | status defines the observed state of SheetExport |  | Optional: \{\} <br /> |


#### SheetExportList



SheetExportList contains a list of SheetExport





| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `SheetExportList` | | |
| `metadata` _[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#listmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  |  |
| `items` _[SheetExport](#sheetexport) array_ |  |  |  |


#### SheetExportSpec



SheetExportSpec describes a read-only mirror of a NetworkScope in a Google Sheet.



_Appears in:_
- [SheetExport](#sheetexport)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `scopeRef` _string_ | scopeRef is the NetworkScope whose subnets are exported. |  | MaxLength: 253 <br />MinLength: 1 <br />Required: \{\} <br /> |
| `spreadsheetID` _string_ | spreadsheetID is the ID from the spreadsheet URL<br />(docs.google.com/spreadsheets/d/<spreadsheetID>/edit). The spreadsheet must be shared<br />with the service account as an editor. |  | Required: \{\} <br /> |
| `sheetName` _string_ | sheetName is the tab that holds the inventory. It is created if missing and fully<br />rewritten on every export, so people should not keep their own data in it. | Subnets | Optional: \{\} <br /> |
| `credentialsSecretRef` _[SecretKeyRef](#secretkeyref)_ | credentialsSecretRef holds the Google service account JSON. |  | Required: \{\} <br /> |
| `extraTagColumns` _string array_ | extraTagColumns adds one column per tag key, after the standard columns. |  | Optional: \{\} <br /> |
| `refreshInterval` _[Duration](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#duration-v1-meta)_ | refreshInterval is how often the sheet is rewritten. | 5m | Optional: \{\} <br /> |


#### SheetExportStatus



SheetExportStatus is the observed state of the export.



_Appears in:_
- [SheetExport](#sheetexport)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `observedGeneration` _integer_ | observedGeneration is the generation of the spec that was last exported. |  | Optional: \{\} <br /> |
| `lastExportTime` _[Time](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#time-v1-meta)_ | lastExportTime is when the sheet was last written. |  | Optional: \{\} <br /> |
| `rows` _integer_ | rows is the number of subnets in the sheet, excluding the header. |  | Optional: \{\} <br /> |
| `url` _string_ | url links to the exported sheet. |  | Optional: \{\} <br /> |
| `conditions` _[Condition](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#condition-v1-meta) array_ | conditions: Ready is True when the last export succeeded. |  | Optional: \{\} <br /> |


#### SkipRule



SkipRule keeps the policy away from resources somebody else owns. Terraform is the usual
reason: tagging a resource it manages makes the next plan want to remove the tags again.



_Appears in:_
- [AutoImportPolicy](#autoimportpolicy)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `tagKey` _string_ | tagKey, when present on the resource, skips it. With tagValue set, only that value skips. |  | Optional: \{\} <br /> |
| `tagValue` _string_ | tagValue narrows tagKey to one value. |  | Optional: \{\} <br /> |
| `principalPrefix` _string_ | principalPrefix skips resources created by a matching principal, e.g. a CI role. |  | Optional: \{\} <br /> |


#### Subnet



Subnet is a subnet discovered by a NetworkScope. It is read-only for users. On AWS the
object is named after the subnet ID.



_Appears in:_
- [SubnetList](#subnetlist)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `Subnet` | | |
| `metadata` _[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#objectmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  | Optional: \{\} <br /> |
| `spec` _[SubnetSpec](#subnetspec)_ | spec identifies the subnet |  | Required: \{\} <br /> |
| `status` _[SubnetStatus](#subnetstatus)_ | status is the observed state of the subnet |  | Optional: \{\} <br /> |


#### SubnetAllocation



SubnetAllocation is one reserved CIDR and, once created, its subnet.



_Appears in:_
- [SubnetClaimStatus](#subnetclaimstatus)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `name` _string_ | name of the subnet the allocation is for: namePrefix and the zone suffix. It is the Name<br />tag the created subnet carries, and the key of the list. |  | MinLength: 1 <br /> |
| `zone` _string_ | zone the subnet is for, on providers with zonal subnets. |  | Optional: \{\} <br /> |
| `cidrBlock` _string_ | cidrBlock reserved for it. |  |  |
| `subnetID` _string_ | subnetID is the provider ID of the subnet once it exists. |  | Optional: \{\} <br /> |
| `state` _string_ | state is Pending, Created or Failed. |  | Optional: \{\} <br /> |
| `error` _string_ | error is the last creation error, empty otherwise. |  | Optional: \{\} <br /> |


#### SubnetClaim



SubnetClaim requests subnets in a network: the operator reserves free CIDRs and, in Create
mode, creates the subnets with the organization's tags. Subnets are never deleted by the
operator: deleting a claim leaves them in place.



_Appears in:_
- [SubnetClaimList](#subnetclaimlist)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `SubnetClaim` | | |
| `metadata` _[ObjectMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#objectmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  | Optional: \{\} <br /> |
| `spec` _[SubnetClaimSpec](#subnetclaimspec)_ | spec defines the desired subnets |  | Required: \{\} <br /> |
| `status` _[SubnetClaimStatus](#subnetclaimstatus)_ | status defines the observed state of SubnetClaim |  | Optional: \{\} <br /> |


#### SubnetClaimList



SubnetClaimList contains a list of SubnetClaim





| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `SubnetClaimList` | | |
| `metadata` _[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#listmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  |  |
| `items` _[SubnetClaim](#subnetclaim) array_ |  |  |  |


#### SubnetClaimSpec



SubnetClaimSpec asks for subnets in a network.



_Appears in:_
- [SubnetClaim](#subnetclaim)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `scopeRef` _string_ | scopeRef is the NetworkScope whose inventory and credentials are used. The account and<br />region below must be part of that scope, and the claim's provider is the scope's. |  | MaxLength: 253 <br />MinLength: 1 <br />Required: \{\} <br /> |
| `account` _string_ | account is the account that owns the network, in the provider's format (for AWS the<br />12-digit account ID). |  | MinLength: 1 <br />Required: \{\} <br /> |
| `region` _string_ | region of the network. |  | Required: \{\} <br /> |
| `networkID` _string_ | networkID is the network the subnets are carved out of, as its provider ID (for AWS the<br />VPC ID). It must be discovered by the scope. |  | MinLength: 1 <br />Required: \{\} <br /> |
| `prefixLength` _integer_ | prefixLength is the size of every subnet, e.g. 24 for a /24. The provider decides the<br />range; AWS allows 16 to 28. |  | Maximum: 32 <br />Minimum: 1 <br />Required: \{\} <br /> |
| `zones` _string array_ | zones lists the zones to create one subnet in each. Required for providers whose<br />subnets are zonal: on AWS one to six availability zones, e.g. eu-central-1a. |  | MaxItems: 6 <br />Optional: \{\} <br /> |
| `mode` _[ClaimMode](#claimmode)_ | mode is Allocate (reserve CIDRs only) or Create (also create the subnets). | Create | Enum: [Allocate Create] <br />Optional: \{\} <br /> |
| `owner` _string_ | owner, env and tier become the owner, env and tier tags (hs/owner, hs/env and hs/tier on<br />AWS). |  | Required: \{\} <br /> |
| `env` _string_ |  |  | Optional: \{\} <br /> |
| `tier` _string_ |  |  | Optional: \{\} <br /> |
| `namePrefix` _string_ | namePrefix is the Name tag; the zone suffix is appended, e.g. payments-a. Defaults to the<br />claim name. |  | Optional: \{\} <br /> |
| `tags` _object (keys:string, values:string)_ | tags are added to every subnet. The owner, env and tier tags above win on conflict. |  | Optional: \{\} <br /> |
| `aws` _[AWSClaimOptions](#awsclaimoptions)_ | aws holds the settings of subnets created on AWS. Only valid when the scope's provider is<br />AWS. |  | Optional: \{\} <br /> |


#### SubnetClaimStatus



SubnetClaimStatus is the observed state of the claim.



_Appears in:_
- [SubnetClaim](#subnetclaim)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `observedGeneration` _integer_ | observedGeneration is the spec generation the status refers to. |  | Optional: \{\} <br /> |
| `allocations` _[SubnetAllocation](#subnetallocation) array_ | allocations, one per subnet. CIDRs stay stable once reserved. |  | Optional: \{\} <br /> |
| `conditions` _[Condition](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#condition-v1-meta) array_ | conditions: Allocated is True when every subnet has a CIDR; Ready is True when the claim<br />is fulfilled (subnets created, or allocated in Allocate mode). |  | Optional: \{\} <br /> |


#### SubnetList



SubnetList contains a list of Subnet





| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `apiVersion` _string_ | `network.hypersurgery.dev/v1` | | |
| `kind` _string_ | `SubnetList` | | |
| `metadata` _[ListMeta](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#listmeta-v1-meta)_ | Refer to Kubernetes API documentation for fields of `metadata`. |  |  |
| `items` _[Subnet](#subnet) array_ |  |  |  |


#### SubnetSpec



SubnetSpec identifies the observed subnet. It is written by the operator, not by users.



_Appears in:_
- [Subnet](#subnet)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `provider` _[Provider](#provider)_ | provider is the provider of the scope that discovered the subnet. |  | Enum: [AWS] <br /> |
| `id` _string_ | id is the provider's ID of the subnet; for AWS the subnet ID. |  |  |
| `networkID` _string_ | networkID is the ID of the network the subnet belongs to; for AWS the VPC ID. |  |  |
| `account` _string_ | account is the account that owns the subnet. |  |  |
| `region` _string_ | region of the subnet. |  |  |


#### SubnetStatus



SubnetStatus is the observed state of the subnet.



_Appears in:_
- [Subnet](#subnet)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `name` _string_ | name is the value of the Name tag. |  | Optional: \{\} <br /> |
| `state` _string_ | state is the provider's state of the subnet. |  | Optional: \{\} <br /> |
| `cidrBlock` _string_ | cidrBlock is the IPv4 CIDR block. |  | Optional: \{\} <br /> |
| `ipv6CIDRBlocks` _string array_ | ipv6CIDRBlocks are the associated IPv6 CIDR blocks. |  | Optional: \{\} <br /> |
| `zone` _string_ | zone is the zone of a zonal subnet, e.g. the AWS availability zone eu-central-1a. Empty<br />for providers whose subnets are regional. |  | Optional: \{\} <br /> |
| `totalIPs` _integer_ | totalIPs is the number of usable IPv4 addresses, as the provider counts them (AWS<br />reserves 5 per subnet). Unset when unknown. |  | Optional: \{\} <br /> |
| `availableIPs` _integer_ | availableIPs is the number of free IPv4 addresses. Unset when the provider could not<br />report it, which is not the same as zero. |  | Optional: \{\} <br /> |
| `utilizationPercent` _integer_ | utilizationPercent is the share of used IPv4 addresses, 0-100. Unset when unknown. |  | Optional: \{\} <br /> |
| `owner` _string_ | owner is read from the owner tag configured in the NetworkScope. |  | Optional: \{\} <br /> |
| `env` _string_ | env is read from the env tag configured in the NetworkScope. |  | Optional: \{\} <br /> |
| `tier` _string_ | tier is read from the tier tag configured in the NetworkScope. |  | Optional: \{\} <br /> |
| `ownershipSource` _[OwnershipSource](#ownershipsource)_ | ownershipSource says where owner, env and tier were read from: Subnet (the subnet's own<br />metadata) or Network (inherited from its network, on providers whose subnets cannot<br />carry metadata of their own). |  | Optional: \{\} <br /> |
| `tags` _object (keys:string, values:string)_ | tags are the subnet's tags at the provider. |  | Optional: \{\} <br /> |
| `missingTags` _string array_ | missingTags lists required tag keys that are absent or empty. |  | Optional: \{\} <br /> |
| `aws` _[AWSSubnetStatus](#awssubnetstatus)_ | aws holds what only an AWS subnet has. |  | Optional: \{\} <br /> |


#### TagKeys



TagKeys names the tags that carry inventory metadata. Unset keys default per provider: on
AWS hs/owner, hs/env and hs/tier.



_Appears in:_
- [NetworkScopeSpec](#networkscopespec)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `owner` _string_ | owner is the tag key holding the owning team. |  | Optional: \{\} <br /> |
| `env` _string_ | env is the tag key holding the environment. |  | Optional: \{\} <br /> |
| `tier` _string_ | tier is the tag key holding the subnet tier (public, private, db, ...). |  | Optional: \{\} <br /> |


#### TargetStatus



TargetStatus reports the last sync of one account/region pair.



_Appears in:_
- [NetworkScopeStatus](#networkscopestatus)

| Field | Description | Default | Validation |
| --- | --- | --- | --- |
| `account` _string_ | account is the account ID. |  |  |
| `region` _string_ | region is the region. |  |  |
| `networks` _integer_ | networks is the number of networks discovered. |  |  |
| `subnets` _integer_ | subnets is the number of subnets discovered. |  |  |
| `unmanagedNetworks` _integer_ | unmanagedNetworks is the number of networks the selector does not match. |  | Optional: \{\} <br /> |
| `unmanagedSubnets` _integer_ | unmanagedSubnets is the number of subnets inside those networks. |  | Optional: \{\} <br /> |
| `unmanagedIDs` _string array_ | unmanagedIDs are the network and subnet IDs that were outside the selector at the last<br />successful sync. The operator keeps them here rather than in memory so that a restart or<br />a change of leader can tell a resource that appeared since then from one that was<br />already known — otherwise every known unmanaged resource would be reported as new again. |  | Optional: \{\} <br /> |
| `lastSyncTime` _[Time](https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.34/#time-v1-meta)_ | lastSyncTime is the time of the last successful sync. |  | Optional: \{\} <br /> |
| `error` _string_ | error is the last sync error, empty when the last sync succeeded. |  | Optional: \{\} <br /> |


